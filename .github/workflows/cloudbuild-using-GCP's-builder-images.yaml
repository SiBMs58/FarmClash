steps:
  # Step 1: Build the Docker image
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/home/app/FarmClash:$SHORT_SHA', '.']

  # Step 2: Push the image to Google Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/home/app/FarmClash:$SHORT_SHA']

  # Step 3: Deploy the image to Cloud Run (or GKE, if preferred)
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args: ['run', 'deploy', 'my-app-service', '--image', 'gcr.io/$PROJECT_ID/my-app:$SHORT_SHA', '--region', 'europe-west1', '--platform', 'managed']
